rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow everything globally for debugging
    match /{document=**} {
      allow read, write, delete: if true;
    }
  }
}
